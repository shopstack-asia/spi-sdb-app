import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/sonner";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "SPI Safe Deposit Box - Premium Vault Services",
  description: "Secure your valuables with SPI's premium safe deposit box services. Professional vault storage, meeting rooms, and exclusive member benefits.",
  keywords: "safe deposit box, vault, secure storage, SPI, Thailand, premium storage",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="font-sans antialiased">
        {children}
        <Toaster />
      </body>
    </html>
  );
}
